// lib/screens/ask_for_common.dart
import 'package:flutter/material.dart';

/// ===== Design Tokens =====
// 포커스/액센트(텍스트필드 포커스 보더 등): #67E8F9
const kAccent = Color(0xFF5DADE2);

// 주요 텍스트: #1F2937
const kTextPrimary = Color(0xFF1F2937);

// 보조 텍스트
const kTextMuted = Color(0xFF6B7280);

// 페이지 배경
const kPageBg = Color.fromARGB(255, 255, 255, 255);

// 얇은 구분선: #E5E7EB
const kDivider = Color(0xFFE5E7EB);

// 입력 필드 배경: #F3F4F6  (= bg-gray-100)
const kInputBg = Color(0xFFF3F4F6);

const kWhite = Colors.white;
const kHeartRed = Color(0xFFFF4D4D);
const kHeartGrey = Color(0xFFD1D5DB);
const kUrgentRed = Color.fromARGB(255, 187, 32, 32);

/// 이미지/카드 여백 관련
const kThumbSize = 100.0; // 썸네일 크기
const kCardPad = 10.0; // 카드 패딩
