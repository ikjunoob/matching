// lib/screens/hotusers.dart
import 'package:flutter/material.dart';

class HotUsersScreen extends StatelessWidget {
  const HotUsersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "매칭 유저 탭(더미)",
        style: TextStyle(fontSize: 20, fontWeight: FontWeight.bold),
      ),
    );
  }
}
